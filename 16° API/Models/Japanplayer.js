//arquivo importante para que a requisição com o banco de dados seja realizada
const mongoose = require('mongoose');

const Japanplayer = ('/ljl', {
  id: Number,
  nickName: String,
  lane: String,
  nationality: String,
  team: String,
  age: String
});

modules.exports = Japanplayer;
